[build]
builder = "nixpacks"
buildCommand = "npm install"

[deploy]
startCommand = "node --import tsx/esm server/index.js"
healthcheckPath = "/api/ping"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[volumes]]
mountPath = "/app/data"
